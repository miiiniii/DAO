<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "com.virtualAsset.webServer.dataAccessObject.AuthDAO">
	<resultMap type="com.virtualAsset.webServer.entity.AuthEntity" id="AuthResult">
		<result column="user_id" property="id"/>
		<result column="user_name" property="name"/>
		<result column="user_nick" property="nick"/>
	</resultMap>
	<select id="getPassword" resultType="String">
		select
			"user_pw"
		from
			"user"
		where
			"user_id" = #{id}
	</select>
	
	<select id="getUserInfo" resultMap="AuthResult">
		select
			"user_id","user_name","user_nick"
		from
			"user"
		where
			"user_id" = #{id}
	</select>
</mapper>
